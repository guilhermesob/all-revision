#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("arquivo virado apenas para fazer o teste da pasta para salvar os arquivos de estrutura de dados!");
   return 0;
}
